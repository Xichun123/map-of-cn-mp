.page {
  padding: var(--page-top) var(--page-x) 110rpx;
}

.mast-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
}
.mast-name {
  font-size: 20rpx;
  font-weight: 700;
  letter-spacing: 6rpx;
}
.mast-vol {
  font-size: 20rpx;
  font-weight: 700;
  letter-spacing: 3rpx;
  color: var(--muted);
}
.mast .rule {
  margin-top: 12rpx;
}
.hero {
  padding: var(--hero-top) 0 var(--hero-bottom);
}
.hero .h1 {
  font-size: var(--hero-title);
  line-height: 1;
  margin-top: 16rpx;
}
.hero .br {
  height: 0;
}
.lede {
  margin-top: 22rpx;
  max-width: 560rpx;
  font-size: var(--hero-lede);
  line-height: 1.75;
  color: var(--ink-2);
}

/* 无权限 */
.gate {
  padding: 120rpx 0;
  text-align: center;
}
.gate-zh {
  font-size: 64rpx;
}
.gate-sub {
  color: var(--muted);
  font-size: 28rpx;
  margin: 16rpx 0 40rpx;
}
.gate-btn {
  display: inline-block;
  border: 1rpx solid var(--ink);
  padding: 16rpx 40rpx;
  font-size: 28rpx;
  background: var(--ink);
  color: var(--bg);
  border-radius: var(--radius-md);
}

/* Tabs */
.tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin: 32rpx 0 28rpx;
}
.tab {
  display: flex;
  flex: 1 1 126rpx;
  align-items: center;
  justify-content: center;
  min-width: 126rpx;
  max-width: 100%;
  text-align: center;
  padding: 14rpx 22rpx;
  font-size: 22rpx;
  letter-spacing: 1rpx;
  line-height: 1.35;
  white-space: normal;
  overflow-wrap: anywhere;
  border: 1rpx solid var(--line);
  background: var(--surface);
  color: var(--ink-2);
  transition: 0.18s;
  box-sizing: border-box;
  border-radius: var(--radius-full);
}
.todo-head {
  display: flex;
  gap: 14rpx;
  margin-bottom: 22rpx;
}
.todo-stat {
  flex: 1;
  padding: 20rpx 12rpx;
  border: 1rpx solid var(--line);
  background: var(--surface);
  text-align: center;
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}
.todo-num {
  display: block;
  font-size: 42rpx;
  font-weight: 700;
  color: var(--ink);
  line-height: 1;
}
.todo-label {
  display: block;
  margin-top: 8rpx;
  font-size: 20rpx;
  color: var(--muted);
}
.backup-box {
  margin: 0 0 24rpx;
  padding: 22rpx;
  border: 1rpx solid var(--line);
  background: var(--surface);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}
.backup-box.bad {
  border-color: rgba(149, 58, 44, 0.55);
}
.backup-box.busy {
  border-color: rgba(105, 117, 84, 0.45);
}
.backup-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}
.backup-title {
  min-width: 0;
  display: flex;
  align-items: center;
  gap: 12rpx;
}
.backup-name {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--ink);
}
.backup-state {
  flex: none;
  padding: 4rpx 12rpx;
  font-size: 20rpx;
  color: var(--muted);
  border: 1rpx solid var(--line);
  border-radius: var(--radius-full);
}
.backup-box.bad .backup-state {
  color: #953a2c;
  border-color: rgba(149, 58, 44, 0.55);
}
.backup-action {
  flex: none;
  padding: 10rpx 18rpx;
  font-size: 22rpx;
  color: var(--bg);
  background: var(--ink);
  border-radius: var(--radius-full);
}
.backup-detail {
  margin-top: 12rpx;
  font-size: 22rpx;
  line-height: 1.55;
  color: var(--muted);
  overflow-wrap: anywhere;
}
.backup-metrics {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
  margin-top: 18rpx;
}
.backup-metric {
  min-width: 0;
  padding: 14rpx 8rpx;
  text-align: center;
  border: 1rpx solid var(--line-soft);
  border-radius: var(--radius-sm);
}
.backup-num {
  display: block;
  font-size: 34rpx;
  line-height: 1;
  font-weight: 700;
  color: var(--ink);
}
.backup-label {
  display: block;
  margin-top: 8rpx;
  font-size: 19rpx;
  color: var(--muted);
}
.todo-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}
.todo-group-title {
  margin: 0 0 12rpx;
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 2rpx;
  color: var(--muted);
}
.todo-row {
  display: flex;
  gap: 16rpx;
  align-items: flex-start;
  padding: 20rpx;
  border: 1rpx solid var(--line-soft);
  background: var(--surface);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}
.todo-type {
  flex: none;
  padding: 4rpx 12rpx;
  border: 1rpx solid var(--line);
  font-size: 20rpx;
  color: var(--ink);
  border-radius: var(--radius-full);
}
.todo-mid {
  min-width: 0;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}
.todo-title {
  font-size: 26rpx;
  font-weight: 700;
  color: var(--ink);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.todo-desc {
  font-size: 22rpx;
  line-height: 1.45;
  color: var(--muted);
}
.tab.on {
  background: var(--ink);
  border-color: var(--ink);
  color: var(--bg);
  font-weight: 700;
  box-shadow: var(--shadow-card);
}

/* 分类筛选条 */
.catbar {
  display: flex;
  flex-wrap: wrap;
  gap: 14rpx;
  margin-bottom: 24rpx;
}
.catchip {
  display: block;
  max-width: 100%;
  padding: 12rpx 24rpx;
  font-size: 24rpx;
  line-height: 1.35;
  white-space: normal;
  overflow-wrap: anywhere;
  box-sizing: border-box;
  border: 1rpx solid var(--line);
  color: var(--ink-2);
  background: var(--surface);
  border-radius: var(--radius-full);
}
.catchip.on {
  background: var(--ink);
  color: var(--bg);
  border-color: var(--ink);
}

/* 徽标 */
.bdg {
  display: inline-block;
  font-size: 20rpx;
  letter-spacing: 1rpx;
  padding: 3rpx 12rpx;
  margin-right: 10rpx;
  border: 1rpx solid var(--line);
  color: var(--ink-2);
  border-radius: var(--radius-sm);
}
.bdg.rec {
  background: var(--ink);
  color: var(--bg);
  border-color: var(--ink);
}
.bdg.muted {
  color: var(--faint);
  border-style: dashed;
}
.bdg.owner {
  color: var(--muted);
  background: var(--surface);
}
.bdg.owner.mine {
  color: var(--ink);
  border-color: var(--ink);
}

/* 菜品卡 */
.dish-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}
.d-card {
  display: flex;
  gap: 24rpx;
  background: var(--surface);
  border: 1rpx solid var(--line-soft);
  box-shadow: var(--shadow-card);
  padding: 24rpx;
  border-radius: var(--radius-md);
}
.d-card.off {
  opacity: 0.55;
}
.d-thumb {
  width: 120rpx;
  height: 120rpx;
  flex: none;
  border: 1rpx solid var(--line);
  background: var(--paper);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border-radius: var(--radius-sm);
}
.d-img {
  width: 100%;
  height: 100%;
}
.d-thumb-ph {
  font-size: 56rpx;
  color: var(--faint);
}
.d-mid {
  flex: 1;
  min-width: 0;
}
.d-name {
  font-size: 28rpx;
  font-weight: 700;
}
.d-badges {
  margin: 8rpx 0 4rpx;
}
.d-desc {
  font-size: 24rpx;
  color: var(--muted);
  margin-top: 4rpx;
}
.d-price {
  font-size: 28rpx;
  font-weight: 700;
  margin-top: 8rpx;
}
.d-ops,
.c-ops {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 8rpx;
  flex: none;
}
.op-move {
  display: flex;
  gap: 8rpx;
  margin-bottom: 2rpx;
}
.mv {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  border: 1rpx solid var(--line-soft);
  font-size: 24rpx;
  color: var(--muted);
  background: var(--paper);
  border-radius: var(--radius-sm);
}
.op {
  font-size: 22rpx;
  color: var(--ink-2);
  border: 1rpx solid var(--line-soft);
  padding: 8rpx 16rpx;
  background: var(--surface);
  border-radius: var(--radius-sm);
}
.op.del {
  color: var(--faint);
  border-color: transparent;
  background: transparent;
}

/* 分类行 */
.cat-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}
.c-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--surface);
  border: 1rpx solid var(--line-soft);
  box-shadow: var(--shadow-card);
  padding: 24rpx 24rpx;
  border-radius: var(--radius-md);
}
.c-row.off {
  opacity: 0.55;
}
.c-mid {
  display: flex;
  align-items: center;
  gap: 14rpx;
  min-width: 0;
  flex: 1;
}
.c-name {
  font-size: 28rpx;
  font-weight: 700;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.c-ops {
  flex-direction: row;
  align-items: center;
  flex: none;
}

/* 新增按钮 */
.add-btn {
  margin-top: 34rpx;
  text-align: center;
  border: 1rpx solid var(--ink);
  padding: 20rpx;
  font-size: 26rpx;
  color: var(--bg);
  background: var(--ink);
  letter-spacing: 1rpx;
  border-radius: var(--radius-md);
}

/* 订单 */
.order-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}
.o-card {
  background: var(--surface);
  border: 1rpx solid var(--line-soft);
  box-shadow: var(--shadow-card);
  padding: 24rpx;
  border-radius: var(--radius-md);
}
.o-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.o-date {
  font-size: 28rpx;
  font-weight: 700;
}
.o-status {
  font-size: 20rpx;
  padding: 6rpx 16rpx;
  border: 1rpx solid var(--line-soft);
  color: var(--ink-2);
  border-radius: var(--radius-full);
}
.o-status.s-done {
  background: var(--ink);
  color: var(--bg);
  border-color: var(--ink);
}
.o-status.s-canceled {
  color: var(--faint);
  border-style: dashed;
}
.o-who {
  font-size: 20rpx;
  color: var(--muted);
  margin-top: 10rpx;
}
.o-sum {
  font-size: 28rpx;
  margin-top: 8rpx;
}
.o-note {
  font-size: 24rpx;
  color: var(--muted);
  font-style: italic;
  margin-top: 6rpx;
}
.o-foot {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-top: 16rpx;
  padding-top: 14rpx;
  border-top: 1rpx solid var(--line-soft);
}
.o-count {
  font-size: 20rpx;
  color: var(--muted);
}
.o-total {
  font-size: 28rpx;
  font-weight: 700;
}

/* 编辑器弹层 */
.mask {
  position: fixed;
  inset: 0;
  background: var(--mask-soft);
  z-index: 100;
  display: flex;
  align-items: flex-end;
}
.sheet {
  width: 100%;
  background: var(--bg);
  padding: 30rpx 40rpx calc(42rpx + env(safe-area-inset-bottom));
  border-top-left-radius: 24rpx;
  border-top-right-radius: 24rpx;
  border-top: 1rpx solid var(--line-soft);
  max-height: 88vh;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  animation: sheetup 0.28s cubic-bezier(0.22, 0.61, 0.36, 1);
  box-shadow: 0 -12rpx 38rpx rgba(27, 23, 18, 0.13);
}
@keyframes sheetup {
  from { transform: translateY(60rpx); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}
.sheet.tall .sheet-scroll {
  max-height: 62vh;
}
.sheet-title {
  font-size: 32rpx;
  font-weight: 700;
  text-align: center;
  padding-bottom: 22rpx;
  margin-bottom: 22rpx;
  border-bottom: 1rpx solid var(--line-soft);
}
.fl {
  font-size: 20rpx;
  letter-spacing: 2rpx;
  color: var(--muted);
  text-transform: uppercase;
  margin: 16rpx 0 8rpx;
}
.fl-row {
  display: flex;
  gap: 24rpx;
}
.fl-col {
  flex: 1;
}
.rec-col {
  flex: none;
}
.fld {
  width: 100%;
  background: var(--surface);
  border: 1rpx solid var(--line-soft);
  min-height: 76rpx;
  padding: 16rpx 20rpx;
  font-size: 28rpx;
  border-radius: var(--radius-sm);
  box-sizing: border-box;
}
.fld.picker {
  color: var(--ink);
}
.ph {
  color: var(--faint);
}
.img-row {
  margin-top: 6rpx;
}
.img-add {
  width: 200rpx;
  height: 200rpx;
  border: 1rpx dashed var(--line);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: var(--muted);
  border-radius: var(--radius-md);
  background: var(--surface);
}
.img-prev {
  position: relative;
  width: 200rpx;
  height: 200rpx;
}
.img-prev-i {
  width: 100%;
  height: 100%;
  border: 1rpx solid var(--line-soft);
  border-radius: var(--radius-md);
}
.img-x {
  position: absolute;
  top: -16rpx;
  right: -16rpx;
  width: 44rpx;
  height: 44rpx;
  line-height: 40rpx;
  text-align: center;
  background: var(--ink);
  color: var(--bg);
  border-radius: 50%;
  font-size: 32rpx;
}
.sheet-ops {
  display: flex;
  gap: 16rpx;
  margin-top: 30rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid var(--line-soft);
}
.sbtn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  font-size: 28rpx;
  border-radius: var(--radius-md);
}
.sbtn.ghost {
  border: 1rpx solid var(--line);
  color: var(--ink-2);
  background: var(--surface);
}
.sbtn.solid {
  background: var(--ink);
  color: var(--bg);
  border: 1rpx solid var(--ink);
}

/* 足迹 / 纪念日 列表 */
.c-mid.col {
  flex-direction: column;
  align-items: flex-start;
  gap: 8rpx;
  flex: 1;
  min-width: 0;
}
.j-prov {
  font-size: 24rpx;
  color: var(--muted);
}
.j-meta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
  color: var(--ink-2);
}
.a-count {
  font-size: 20rpx;
  letter-spacing: 1rpx;
  color: var(--muted);
}
.a-count.count-countdown,
.a-count.count-today {
  color: var(--ink);
  font-weight: 700;
}

/* 编辑器：色调选择 + 坐标 + 多行 */
.area {
  min-height: 120rpx;
  line-height: 1.5;
}
.tone-row {
  display: grid;
  grid-template-columns: repeat(6, minmax(0, 1fr));
  gap: 10rpx;
}
.tone-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16rpx;
  margin-top: 18rpx;
}
.tone-head .fl {
  margin-top: 0;
}
.tone-note {
  flex: none;
  font-size: 22rpx;
  color: var(--faint);
}
.tone-row.compact {
  margin-top: 8rpx;
}
.tone-chip {
  min-width: 0;
  height: 68rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
  font-size: 22rpx;
  line-height: 1;
  border: 1rpx solid var(--line);
  background: var(--surface);
  color: var(--ink-2);
  border-radius: var(--radius-sm);
  box-sizing: border-box;
}
.tone-chip.on {
  background: var(--ink);
  border-color: var(--ink);
  color: var(--bg);
}
.tone-dot {
  flex: none;
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  border: 1rpx solid rgba(27, 23, 18, 0.16);
}
.tone-chip.on .tone-dot {
  border-color: rgba(244, 241, 234, 0.64);
}
.geo-row {
  align-items: center;
  gap: 14rpx;
}
.geo-in {
  flex: 1;
  min-width: 0;
}
.geo-btn {
  flex: none;
  white-space: nowrap;
  padding: 16rpx 24rpx;
  font-size: 24rpx;
  border: 1rpx solid var(--ink);
  background: var(--ink);
  color: var(--bg);
}
.geo-btn-wide {
  margin-top: 14rpx;
  width: 100%;
  box-sizing: border-box;
  min-height: 76rpx;
  padding: 0 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  font-size: 28rpx;
  letter-spacing: 1rpx;
  border: 1rpx solid var(--ink);
  background: var(--ink);
  color: var(--bg);
  line-height: 1.35;
  border-radius: var(--radius-md);
  white-space: normal;
}
.geo-btn-wide.alt {
  background: transparent;
  color: var(--ink);
  border-color: var(--line);
}
.geo-btn-wide.pressed {
  transform: scale(0.985);
}
.geo-btn-wide.busy {
  opacity: 0.68;
}
.rec-line {
  align-items: center;
  justify-content: space-between;
  margin-top: 8rpx;
}

/* 打卡照片墙编辑 */
.photo-hint {
  font-size: 24rpx;
  color: var(--faint);
  padding: 8rpx 0 4rpx;
}
.photo-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  padding: 6rpx 0 4rpx;
}
.photo-cell {
  position: relative;
  width: 150rpx;
  height: 150rpx;
}
.photo-img {
  width: 150rpx;
  height: 150rpx;
  display: block;
  border-radius: var(--radius-sm);
}
.photo-rm {
  position: absolute;
  top: -12rpx;
  right: -12rpx;
  width: 40rpx;
  height: 40rpx;
  line-height: 36rpx;
  text-align: center;
  background: var(--ink);
  color: var(--bg);
  border-radius: 50%;
  font-size: 28rpx;
}
.photo-add {
  width: 150rpx;
  height: 150rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1rpx dashed var(--line);
  font-size: 24rpx;
  color: var(--ink-2);
  border-radius: var(--radius-sm);
  background: var(--surface);
}
.photo-pending {
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  box-sizing: border-box;
  text-align: center;
  padding: 4rpx 0;
  font-size: 20rpx;
  background: rgba(27, 23, 18, 0.72);
  color: var(--bg);
}
.photo-preview-mask {
  position: fixed;
  inset: 0;
  z-index: 130;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 44rpx 32rpx calc(44rpx + env(safe-area-inset-bottom));
  background: rgba(27, 23, 18, 0.82);
}
.photo-preview-panel {
  width: 100%;
  max-width: 680rpx;
}
.photo-preview-stage {
  position: relative;
  width: 100%;
  height: 68vh;
  max-height: 900rpx;
  min-height: 520rpx;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #0f0e0c;
  border-radius: var(--radius-md);
}
.photo-preview-img {
  width: 100%;
  height: 100%;
  display: block;
}
.photo-preview-tone {
  background: var(--surface);
}
.photo-preview-badge {
  position: absolute;
  left: 18rpx;
  top: 18rpx;
  height: 42rpx;
  line-height: 42rpx;
  padding: 0 16rpx;
  border-radius: 4rpx;
  background: rgba(244, 241, 234, 0.92);
  color: var(--ink);
  font-size: 22rpx;
}
.photo-preview-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 18rpx;
}
.photo-preview-btn {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  border-radius: var(--radius-md);
  font-size: 28rpx;
}
.photo-preview-btn.ghost {
  border: 1rpx solid var(--line);
  color: var(--ink-2);
  background: var(--bg);
}
.photo-preview-btn.solid {
  border: 1rpx solid var(--ink);
  color: var(--bg);
  background: var(--ink);
}
.photo-preview-btn.disabled {
  opacity: 0.52;
}
